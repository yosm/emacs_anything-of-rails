Anything of Rails

installation:
  
  (require 'rails)

require:
 - anything
 - find-cmd

usage:
on any rails project buffer

  M-x anything-of-rails

or

  default key assign "C-;"
  if you want to use other key then you set 'anything-of-rails-boot-key before (require 'rails)

  (defvar anything-of-rails-boot-key (kbd "C-;"))
